# @upnext/database — Environment
# Copy to .env.local and fill in the values.
# Used by drizzle-kit scripts (db:push, db:generate, db:studio).

DATABASE_URL=postgresql://postgres:password@localhost:5432/upnext
